{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // Output build files to dist
    "outDir": "dist",
    "allowJs": true
  },
  "include": [
    "src/**/*"
  ],
  "exclude": [
    "**/__tests__"
  ]
}
